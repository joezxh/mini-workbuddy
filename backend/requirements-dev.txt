# FastAPI 核心
fastapi==0.115.0
uvicorn[standard]>=0.31.1
python-multipart>=0.0.9
python-dotenv>=1.0.1

# 数据库
sqlalchemy>=2.0.35
psycopg2-binary>=2.9.10
neo4j>=5.23,<6
alembic==1.13.1
pgvector>=0.3.0

# Redis
redis==5.0.1

# 认证与安全
python-jose[cryptography]==3.3.0
bcrypt>=4.0.0
PyJWT[crypto]>=2.10.1

# 数据验证
pydantic>=2.7.4,<3.0.0
pydantic-settings>=2.1.0

# HTTP 客户端
httpx>=0.26.0

# LangChain & LangGraph
# 已移除 - app/ai/langgraph/ 实验模块已删除
# langchain-core, langchain-text-splitters, langchain-openai>=0.2.0,
# langchain-community, langchain, langgraph>=0.2.0, langgraph-checkpoint,
# langsmith

# Embedding & NLP (CPU 优化)
numpy>=1.24.0
# PyTorch - CPU 版本通过阿里云镜像安装（仅 birt_bert_matcher 使用）
torch>=2.2.0
transformers>=4.40.0
sentence-transformers>=2.7.0
jieba>=0.42.0
scikit-learn>=1.3.0,<2.0.0
onnxruntime>=1.17.0

# 任务调度
apscheduler==3.10.4
celery==5.3.6
kombu==5.3.5

# 工具库
reportlab==4.0.9
python-docx==1.1.0
mistune==3.0.2
openpyxl==3.1.2  # 新增：Excel 文件处理依赖
pdfplumber>=0.10.0  # 新增：PDF 提取依赖
python-pptx>=0.6.23  # 新增：PowerPoint 处理依赖

# 日志
loguru==0.7.2

# ASGI 服务器（生产环境）
gunicorn==21.2.0

# ARQ 轻量异步任务队列
arq>=0.25.0,<0.26.0

# 缓存
cachetools==5.5.0

# 测试
pytest==8.0.1
pytest-asyncio==0.23.5
aiosqlite>=0.20

# YAML 配置
PyYAML>=6.0.1

# 加密（API Key Vault）
cryptography>=42.0.0

# 本体推理（ontology 模块依赖）
owlready2>=0.46

# 推理引擎（P1-P6 多范式推理）
# owl: owlready2（已在上方声明）
# prolog: pyswip（需要系统安装 SWI-Prolog 运行时，下载地址 https://www.swi-prolog.org/download/stable）
pyswip>=0.3.3
# sparql: rdflib（RDF 三元组 + SPARQL 查询）
rdflib>=7.0.0
# fuzzy: scikit-fuzzy（Mamdani 模糊推理，不可用时降级为线性公式）
scikit-fuzzy>=0.5.0
# bayesian / fuzzy 依赖 numpy（已在上方声明）

# Graphiti 长期记忆
graphiti-core>=0.5.0

# MCP 协议（知识库 MCP Server）
# agentscope 2.0.4 需要 streamable_http_client（mcp>=1.15.0 引入）
mcp>=1.15.0

# AgentScope 2.x 运行时
agentscope>=2.0,<3.0
ripgrep>=15.0.0
